@{
    ViewData["Title"] = "Download";
}


@model IEnumerable<Sincronizador_de_legendas.Models.ArquivosModel>
<h2>Legendas formatadas</h2>
<p>
</p>
<table>
    <tr>
        <th>
            Nome
        </th>
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.ActionLink("Download", "Download", new { nome = item.Nome })
            </td>
        </tr>
    }
</table>